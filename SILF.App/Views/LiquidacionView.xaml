<!-- Ruta: D:\ARCHIVOS\POTOSI\SILF\SILF.App\Views\LiquidacionView.xaml -->
<UserControl x:Class="SILF.App.Views.LiquidacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="Lbl" TargetType="TextBlock"><Setter Property="FontSize" Value="12"/><Setter Property="VerticalAlignment" Value="Center"/><Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/></Style>
        <Style x:Key="Val" TargetType="TextBlock"><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="VerticalAlignment" Value="Center"/><Setter Property="HorizontalAlignment" Value="Right"/><Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/></Style>
        <Style x:Key="Tot" TargetType="TextBlock" BasedOn="{StaticResource Val}"><Setter Property="FontSize" Value="14"/><Setter Property="FontWeight" Value="Bold"/></Style>
        <Style x:Key="Sep" TargetType="Border"><Setter Property="BorderThickness" Value="0,0,0,1"/><Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/><Setter Property="Margin" Value="0,4"/></Style>
    </UserControl.Resources>
    <DockPanel>
        <!-- BARRA SUPERIOR -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Button HorizontalAlignment="Left" Command="{Binding VolverCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" md:ButtonAssist.CornerRadius="6" Padding="12,6">
                <StackPanel Orientation="Horizontal"><md:PackIcon Kind="ArrowLeft" Width="18" Height="18" Margin="0,0,6,0"/><TextBlock Text="Volver a lista" VerticalAlignment="Center"/></StackPanel></Button>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Command="{Binding ExportarPdfCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" md:ButtonAssist.CornerRadius="6" Padding="12,6" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="FilePdfBox" Width="18" Height="18" Margin="0,0,6,0" Foreground="#C62828"/><TextBlock Text="PDF" VerticalAlignment="Center"/></StackPanel></Button>
                <Button Command="{Binding ImprimirCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" md:ButtonAssist.CornerRadius="6" Padding="12,6">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Printer" Width="18" Height="18" Margin="0,0,6,0"/><TextBlock Text="Imprimir" VerticalAlignment="Center"/></StackPanel></Button>
            </StackPanel>
        </Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <md:Card Padding="32" md:ElevationAssist.Elevation="Dp1" HorizontalAlignment="Center" Width="900">
                <StackPanel>
                    <TextBlock Text="LIQUIDACIÓN DE MINERALES" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,4" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Border Style="{StaticResource Sep}" Margin="0,0,0,16"/>

                    <!-- INFO LOTE -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,3"><TextBlock Text="NOMBRE: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoProveedor}" Style="{StaticResource Lbl}"/></StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="CI/NIT: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoCiNit}" Style="{StaticResource Lbl}"/></StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,3"><TextBlock Text="MINA: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoMina}" Style="{StaticResource Lbl}"/></StackPanel>
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,3"><TextBlock Text="COOPERATIVA: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoCooperativa}" Style="{StaticResource Lbl}"/></StackPanel>
                        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,3"><TextBlock Text="TIPO: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoTipoMineral}" Style="{StaticResource Lbl}" FontWeight="Bold" Foreground="{DynamicResource SecondaryHueMidBrush}"/></StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="LOTE N°: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoNumeroLote}" Style="{StaticResource Lbl}" FontWeight="Bold"/></StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="PESO NETO: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBox Text="{Binding InfoPesoNeto, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignTextBox}" FontSize="12" FontWeight="Bold" Width="100" Padding="2" VerticalAlignment="Center" md:TextFieldAssist.DecorationVisibility="Hidden"/><TextBlock Text=" Tn" Style="{StaticResource Lbl}" Opacity="0.6"/></StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="F. INGRESO: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding InfoFechaIngreso, StringFormat='{}{0:dd/MM/yyyy}'}" Style="{StaticResource Lbl}"/></StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="F. LIQUIDACIÓN: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><DatePicker SelectedDate="{Binding FechaLiquidacion}" Style="{StaticResource MaterialDesignDatePicker}" Width="140" FontSize="12"/></StackPanel>
                        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,3"><TextBlock Text="T/C: " Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Text="{Binding TipoCambio, StringFormat='{}{0:N2}'}" Style="{StaticResource Lbl}" FontWeight="Bold"/><TextBlock Text=" Bs/$US" Style="{StaticResource Lbl}" Opacity="0.6"/></StackPanel>
                    </Grid>
                    <Border Style="{StaticResource Sep}"/>

                    <!-- LEYES + COSTO LABORATORIO -->
                    <TextBlock Text="LEYES DEL LABORATORIO" FontSize="13" FontWeight="Bold" Margin="0,12,0,8" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Margin="0,0,6,0" md:HintAssist.Hint="Ley ZN (%)" md:HintAssist.IsFloating="True" Text="{Binding LeyZn, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="1" Margin="6,0" md:HintAssist.Hint="Ley AG (oz/tc)" md:HintAssist.IsFloating="True" Text="{Binding LeyAg, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="2" Margin="6,0" md:HintAssist.Hint="Ley PB (%)" md:HintAssist.IsFloating="True" Text="{Binding LeyPb, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="3" Margin="6,0" md:HintAssist.Hint="% Humedad" md:HintAssist.IsFloating="True" Text="{Binding Humedad, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="4" Margin="6,0,0,0" md:HintAssist.Hint="Costo Lab. (Bs)" md:HintAssist.IsFloating="True" Text="{Binding CostoLaboratorio, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>

                    <!-- COTIZACIONES -->
                    <TextBlock Text="COTIZACIONES DEL DÍA" FontSize="13" FontWeight="Bold" Margin="0,8,0,8" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Margin="0,0,6,0" md:HintAssist.Hint="Precio ZN ($US)" md:HintAssist.IsFloating="True" Text="{Binding CotizacionZn, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="1" Margin="6,0" md:HintAssist.Hint="Precio AG ($US)" md:HintAssist.IsFloating="True" Text="{Binding CotizacionAg, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        <TextBox Grid.Column="2" Margin="6,0,0,0" md:HintAssist.Hint="Precio PB ($US)" md:HintAssist.IsFloating="True" Text="{Binding CotizacionPb, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </Grid>
                    <Border Style="{StaticResource Sep}"/>

                    <!-- CÁLCULO DE PESO -->
                    <TextBlock Text="CÁLCULO DE PESO" FontSize="13" FontWeight="Bold" Margin="0,12,0,6" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="12,8" Margin="0,0,0,4">
                        <StackPanel>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Peso Neto" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Column="1" Text="{Binding InfoPesoNeto, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource Lbl}"><Run Text="(-) Humedad ("/><Run Text="{Binding Humedad, StringFormat='{}{0:N2}', Mode=OneWay}"/><Run Text="%)"/></TextBlock>
                                <TextBlock Grid.Column="1" Text="{Binding PesoHumedad, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}" Foreground="#C62828"/></Grid>
                            <Border Style="{StaticResource Sep}"/>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="= Peso Neto Seco" Style="{StaticResource Lbl}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1" Text="{Binding PesoNetoSeco, StringFormat='{}{0:N2}'}" Style="{StaticResource Tot}"/></Grid>
                        </StackPanel>
                    </Border>

                    <!-- VALOR COMERCIAL -->
                    <TextBlock Text="VALOR COMERCIAL" FontSize="13" FontWeight="Bold" Margin="0,12,0,6" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="12,8" Margin="0,0,0,4">
                        <StackPanel>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Zinc (Seco × Ley ZN × Precio ZN)" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="{Binding ValorBrutoZn, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Plata (Seco × Ley AG × Precio AG)" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="{Binding ValorBrutoAg, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Plomo (Seco × Ley PB × Precio PB)" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="{Binding ValorBrutoPb, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Border Style="{StaticResource Sep}"/>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Total $US" Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding ValorComercialUs, StringFormat='{}{0:N2}'}" Style="{StaticResource Tot}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource Lbl}" FontWeight="Bold" FontSize="14"><Run Text="Total Bs (× T/C "/><Run Text="{Binding TipoCambio, StringFormat='{}{0:N2}', Mode=OneWay}"/><Run Text=")"/></TextBlock>
                                <TextBlock Grid.Column="1" Text="{Binding ValorComercialBs, StringFormat='{}{0:N2}'}" Style="{StaticResource Tot}" FontSize="16"/></Grid>
                        </StackPanel>
                    </Border>

                    <!-- DEDUCCIONES LEGALES -->
                    <TextBlock Text="DEDUCCIONES LEGALES" FontSize="13" FontWeight="Bold" Margin="0,16,0,6" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="12,8">
                        <StackPanel>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Regalías Mineras" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="6.00%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/><TextBlock Grid.Column="2" Text="{Binding Regalias, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="CNS" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="1.80%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/><TextBlock Grid.Column="2" Text="{Binding Cns, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="COMIBOL" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="1.00%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/><TextBlock Grid.Column="2" Text="{Binding Comibol, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Border Style="{StaticResource Sep}"/>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Subtotal Ded. Legales" Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding TotalDeduccionesLegales, StringFormat='{}{0:N2}'}" Style="{StaticResource Tot}"/></Grid>
                        </StackPanel>
                    </Border>

                    <!-- OTRAS DEDUCCIONES -->
                    <TextBlock Text="OTRAS DEDUCCIONES" FontSize="13" FontWeight="Bold" Margin="0,12,0,6" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="4" Padding="12,8">
                        <StackPanel>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="FENCOMIN" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="0.40%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/><TextBlock Grid.Column="2" Text="{Binding Fencomin, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="FEDECOMIN" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="1" Text="1.00%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/><TextBlock Grid.Column="2" Text="{Binding Fedecomin, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <!-- COOPERATIVA: checkbox + input editable -->
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/><ColumnDefinition Width="80"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{Binding AplicaCooperativa}" Style="{StaticResource MaterialDesignCheckBox}" Margin="-4,0,0,0"/>
                                <TextBlock Grid.Column="1" Text="Cooperativa" Style="{StaticResource Lbl}"/>
                                <TextBox Grid.Column="2" Margin="0,0,8,0" md:HintAssist.Hint="%" IsEnabled="{Binding AplicaCooperativa}"
                                         Text="{Binding PorcentajeCooperativa, UpdateSourceTrigger=PropertyChanged, StringFormat='{}{0:N2}'}"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}" md:TextFieldAssist.DecorationVisibility="Hidden"
                                         FontSize="12" Padding="4,2" HorizontalContentAlignment="Right"/>
                                <TextBlock Grid.Column="3" Text="{Binding MontoCooperativa, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <!-- IUE -->
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/><ColumnDefinition Width="60"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{Binding AplicaIue}" Style="{StaticResource MaterialDesignCheckBox}" Margin="-4,0,0,0"/>
                                <TextBlock Grid.Column="1" Text="Retenciones IUE Bienes" Style="{StaticResource Lbl}"/><TextBlock Grid.Column="2" Text="5.00%" Style="{StaticResource Lbl}" HorizontalAlignment="Right"/>
                                <TextBlock Grid.Column="3" Text="{Binding Iue, StringFormat='{}{0:N2}'}" Style="{StaticResource Val}"/></Grid>
                            <Border Style="{StaticResource Sep}"/>
                            <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="150"/></Grid.ColumnDefinitions>
                                <TextBlock Text="Subtotal Otras Ded." Style="{StaticResource Lbl}" FontWeight="SemiBold"/><TextBlock Grid.Column="1" Text="{Binding TotalOtrasDeducciones, StringFormat='{}{0:N2}'}" Style="{StaticResource Tot}"/></Grid>
                        </StackPanel>
                    </Border>

                    <!-- RESULTADO FINAL -->
                    <Border Background="{DynamicResource PrimaryHueLightBrush}" CornerRadius="6" Padding="20,16" Margin="0,16,0,0">
                        <StackPanel>
                            <Grid Margin="0,2"><TextBlock Text="VALOR COMERCIAL Bs" FontSize="13" Foreground="{DynamicResource PrimaryHueDarkBrush}"/><TextBlock Text="{Binding ValorComercialBs, StringFormat='{}{0:N2}'}" HorizontalAlignment="Right" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryHueDarkBrush}"/></Grid>
                            <Grid Margin="0,4"><TextBlock Text="(-) TOTAL DEDUCCIONES" FontSize="13" Foreground="{DynamicResource PrimaryHueDarkBrush}" Opacity="0.8"/><TextBlock Text="{Binding TotalDeducciones, StringFormat='{}{0:N2}'}" HorizontalAlignment="Right" FontSize="14" FontWeight="SemiBold" Foreground="#C62828"/></Grid>
                            <Border Height="2" Background="{DynamicResource PrimaryHueDarkBrush}" Margin="0,10"/>
                            <Grid Margin="0,10,0,2"><TextBlock Text="LÍQUIDO PAGABLE Bs" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource PrimaryHueDarkBrush}"/><TextBlock Text="{Binding LiquidoPagable, StringFormat='{}{0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" FontSize="24" Foreground="{DynamicResource PrimaryHueDarkBrush}"/></Grid>
                            <Grid Margin="0,0,0,6"><TextBlock Text="LÍQUIDO PAGABLE $US" FontSize="12" Foreground="{DynamicResource PrimaryHueDarkBrush}" Opacity="0.6"/><TextBlock Text="{Binding LiquidoPagableUs, StringFormat='{}{0:N2}'}" HorizontalAlignment="Right" FontSize="13" Foreground="{DynamicResource PrimaryHueDarkBrush}" Opacity="0.6"/></Grid>
                            <Border Background="{DynamicResource PrimaryHueDarkBrush}" CornerRadius="4" Padding="12,8" Margin="0,4,0,0" Opacity="0.08"/>
                            <TextBlock Text="{Binding MontoLiteral}" FontSize="11" FontWeight="SemiBold" FontStyle="Italic" Foreground="{DynamicResource PrimaryHueDarkBrush}" TextWrapping="Wrap" Margin="0,-28,0,0" Padding="12,8"/>
                        </StackPanel>
                    </Border>

                    <!-- CONTROL DE PAGO -->
                    <Border BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" CornerRadius="6" Padding="16,12" Margin="0,12,0,0">
                        <StackPanel>
                            <TextBlock Text="CONTROL DE PAGO" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource MaterialDesignBodyLight}" Margin="0,0,0,8"/>
                            <Grid Margin="0,4"><TextBlock Text="Anticipo entregado" Style="{StaticResource Lbl}"/><TextBlock Text="{Binding AnticipoMonto, StringFormat='Bs {0:N2}'}" HorizontalAlignment="Right" Style="{StaticResource Lbl}" FontWeight="SemiBold"/></Grid>
                            <Border Style="{StaticResource Sep}"/>
                            <Grid Margin="0,4"><TextBlock Text="Saldo por pagar" Style="{StaticResource Lbl}" FontWeight="Bold" FontSize="14"/><TextBlock Text="{Binding SaldoPagar, StringFormat='Bs {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource PrimaryHueMidBrush}"/></Grid>
                        </StackPanel>
                    </Border>

                    <!-- Observaciones -->
                    <TextBox Margin="0,16,0,0" md:HintAssist.Hint="Observaciones" md:HintAssist.IsFloating="True" Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60" MaxHeight="100"/>
                    <Border Background="#FFEBEE" CornerRadius="4" Padding="12,8" Margin="0,8,0,0" Visibility="{Binding TieneError, Converter={StaticResource BoolToVis}}"><TextBlock Text="{Binding MensajeError}" Foreground="#C62828" TextWrapping="Wrap"/></Border>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="GUARDAR LIQUIDACIÓN" Command="{Binding GuardarCommand}" md:ButtonAssist.CornerRadius="6" Padding="24,8" FontWeight="Bold">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}"><Setter Property="IsEnabled" Value="True"/><Style.Triggers><DataTrigger Binding="{Binding Cargando}" Value="True"><Setter Property="IsEnabled" Value="False"/></DataTrigger></Style.Triggers></Style></Button.Style></Button>
                    </StackPanel>
                </StackPanel>
            </md:Card>
        </ScrollViewer>
    </DockPanel>
</UserControl>
